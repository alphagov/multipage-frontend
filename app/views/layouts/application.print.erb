<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title><%= @content.title %> - GOV.UK</title>
  <% if @content.description.present? %>
    <meta name="description" content="<%= @content.description %>">
  <% end %>

  <%= yield :extra_headers %>
</head>
<body>
  <div id="wrapper" class="<%= wrapper_css_class(@content) %>">
    <main role="main">
      <%= yield %>
    </main>
  </div>
</body>
</html>
