<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title><%= @presenter.title %> - GOV.UK</title>
  <% if @presenter.description.present? %>
    <meta name="description" content="<%= @presenter.description %>">
  <% end %>

  <%= yield :extra_headers %>
</head>
<body>
  <main id="wrapper" role="main" class="<%= @presenter.content_class_dasherized %>">
    <%= yield %>
  </main>
</body>
</html>
